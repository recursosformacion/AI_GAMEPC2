lookup:
  method: GET
  path: /cgibin/make-table.cgi
  query:
    searchingfor: "{query}"

search:
  method: GET
  path: /cgibin/make-table.cgi
  query:
    searchingfor: "{query}"

resource:
  method: GET
  path: /cgibin/piece-info.cgi
  query:
    id: "{id}"

download:
  method: GET
  path: /ftp/{path}
