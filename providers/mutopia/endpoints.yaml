lookup:
  method: GET
  path: /search
  query:
    q: "{query}"

search:
  method: GET
  path: /search
  query:
    q: "{query}"

resource:
  method: GET
  path: /ftp/{path}

download:
  method: GET
  path: /ftp/{path}
