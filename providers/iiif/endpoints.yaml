search:
  method: GET
  path: /search
  query:
    q: "{query}"
    page: "{page}"
    per_page: "{limit}"

lookup:
  method: GET
  path: /search
  query:
    q: "{query}"
    page: "1"
    per_page: "10"

resource:
  method: GET
  path: /manifests/{id}

download:
  method: GET
  path: /manifests/{id}/canvas/{canvas_id}/content