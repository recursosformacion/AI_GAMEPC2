lookup:
  method: GET
  path: /search
  query:
    q: "{query}"

search:
  method: GET
  path: /search
  query:
    q: "{query}"

resource:
  method: GET
  path: /catalog/{id}

download:
  method: GET
  path: /catalog/{id}/download
