lookup:
  method: GET
  path: /search
  query:
    q: "{query}"

search:
  method: GET
  path: /search
  query:
    q: "{query}"

resource:
  method: GET
  path: /data/{path}

download:
  method: GET
  path: /data/{path}
