id: kernscores
name: KernScores (Humdrum)
version: 1.3
enabled: true
priority: 100
base_url: https://kern.humdrum.org
native_formats: [kern]
conversion: kern (Humdrum) -> MusicXML (fase posterior; se entregaría el enlace del .krn)
authentication:
  type: none
protocol: github         # definido: existe KernScoresFetcher; NO cableado (sin mirror GitHub verificado/buscable)
contract:
  version: 1.3
  search_endpoint: search
  lookup_endpoint: lookup
  resource_endpoint: resource
  download_endpoint: download
