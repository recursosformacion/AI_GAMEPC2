{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "osap-api",
      "type": "shell",
      "command": "python -m uvicorn --factory src.osap.api.platform_app:create_platform_app --host 127.0.0.1 --port 8001",
      "options": { "cwd": "${workspaceFolder}" },
      "isBackground": true,
      "presentation": { "reveal": "never", "panel": "dedicated", "group": "osap", "clear": false },
      "runOptions": { "runOn": "folderOpen" },
      "problemMatcher": []
    }
  ]
}
